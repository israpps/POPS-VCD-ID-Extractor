cmake_minimum_required(VERSION 3.10)

# set the project name
project(POPS-VCD-ID-Extractor)

# add the executable
add_executable(POPS-VCD-ID-Extractor main.cpp
                               resource.rc
                               )
